# SearchNearbyRestaurantAPI_AWS_Lambda
A simple restaurant search API built with **Node.js** and deployed on **AWS Lambda**.  
It uses the **Google Places API** to find nearby restaurants based on location data.

## 📦 Features
- Search nearby restaurants using latitude and longitude
- Integrates with Google Places API
- Returns JSON-formatted restaurant data
- Lightweight and serverless (AWS Lambda)

## 🚀 Usage
1. Deploy the function to AWS Lambda
2. Set up API Gateway to trigger the Lambda function
3. Send a GET request with query parameters:

## 🛠️ Tech Stack
- Node.js
- AWS Lambda
- Google Places API
- API Gateway (for endpoint exposure)
